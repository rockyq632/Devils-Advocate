extends Enemy


func _ready() -> void:
	# Call the base enemy ready function
	super._ready()


func _process(delta: float) -> void:
	# Call the base enemy process function
	super._process(delta)


func _physics_process(delta: float) -> void:
	# Call the base enemy physics process function
	super._physics_process(delta)
